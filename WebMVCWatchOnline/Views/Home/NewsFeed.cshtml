@using WebMVCWatchOnline.Models.Entities
@{
    ShopWatchContext shop=new ShopWatchContext();
    var tintuc = shop.News.ToList().Skip(0).Take(10);
}

<div class="wIdget">
    <h3>Tin mới nhất</h3>
    <div class="wIdget-article-list">

        @foreach (var item in tintuc)
        {
            <div class="item">
                <div class="item-header">

                    @* <a href="/Home/ChiTietTinTuc/@item.Id"><img src="@item.Image" alt="Not found" style="wIdth: 50px;height: 50px"></a> *@
                    <a href="#"><img src="@item.Image" alt="Not found" style="wIdth: 50px;height: 50px"></a>
                </div>
                <div class="item-content">
                    @* <h4><a href="/Home/ChiTietTinTuc/@item.Id">@item.Title</a></h4> *@
                    <h4><a href="">@item.Title</a></h4>
                    <span class="item-meta">
                        <a href="#"><i class="fa fa-comment-o"></i>@item.Date</a>
                        @* <a href="#"><i class="fa fa-clock-o"></i>@shop.User_.SingleOrDefault(t=>t.Id== item.Uploader).Name</a> *@
                        @* <a href="#"><i class="fa fa-clock-o"></i>@shop.User_.SingleOrDefault(t=>t.Id== item.Uploader).Name</a> *@
                        @* <i>@item.Content</i> *@
                    </span>
                </div>
            </div>

        }


        @* <a href="/Home/TinTuc" class="button-read-more">Xem thêm</a> *@

    </div>
</div>